# 제주 스마트팜 대시보드 프로젝트 히스토리 및 개발 노트

미래의 유지보수 및 기능 확장을 위해 2026년 8월 17일에 논의된 모든 아키텍처 결정 사항과 트러블슈팅 내역을 상세히 기록합니다.

---

## 1. 투야(Tuya) OpenAPI 연동 아키텍처
*   **통신 방식**: 백그라운드 `server.js` (Express.js)가 Tuya OpenAPI를 프록시(Proxy)하여 호출하고, 프론트엔드(`app.js`)는 `http://[서버IP]:3000/api/...` 경로로 호출하는 **분리형 구조**를 채택했습니다.
*   **기상대(EM3395TY-2) 매핑 이슈 (중요)**
    *   초기에는 `temp_current`나 `windspeed_avg` 등의 데이터 코드를 매핑했으나, 해당 기기에서 실제 데이터를 내려보내주지 않거나 `0`을 리턴하는 문제(Tuya Spec 버그)가 있었습니다.
    *   이를 해결하기 위해 오직 **실외기 센서(External)** 전용 코드인 `temp_current_external`, `humidity_outdoor`, `windspeed_gust`, `rain_rate`, `rain_24h` 만을 1:1로 엄격하게 매핑하도록 수정했습니다.
    *   **풍향 (wind_direction)**: Tuya 모바일 앱에는 방위표시(원형)가 있지만 OpenAPI 명세(DP)에는 해당 코드가 아예 내려오지 않습니다. 따라서 풍속이 0일 때는 수동으로 `'C(Center)'`를 띄워 UI 이질감을 없앴으며, 향후 Tuya에서 `wind_direction` 데이터를 열어주면 곧바로 매핑되도록 코드를 열어두었습니다.

## 2. 모바일/원격 통신 환경 개편
*   **Localtunnel 문제**: 초기에 무료 터널링(`localtunnel`)을 도입했으나, 서비스 최초 접속 시 띄우는 강제 '보안 경고창 HTML'이 `app.js`의 `fetch(switch)` API JSON 응답을 가로채어 파싱 에러(SyntaxError)를 발생시켰습니다.
*   **다이렉트 접속(포트 포워딩)**: 터널링 툴을 완전히 삭제하고, 공유기(SK Broadband)의 **포트 포워딩**을 이용해 외부망(LTE)에서 내부망(PC)의 `192.168.75.132:3000`으로 뚫고 들어오는 직통 고속망을 구성했습니다.
*   **PC 전원 관리**: `powercfg -change -standby-timeout-ac 0` 명령어로 대시보드 서버 역할을 하는 PC가 대기 모드(절전)에 들어가지 않도록 강제 세팅했습니다.

## 3. 멀티-사이트 (4개 농장) 탭 네비게이션 설계
*   서홍동 외에 하예, 하원, 하효동 밭 확장에 대비하여 `index.html` 내부에 `display: none / grid` 트랜지션을 활용한 **4 탭 아키텍처**를 구축했습니다.
*   `app.js`의 `checkConnection()` 로직을 탭 스위칭 이벤트에 결합시켜, 탭을 넘길 때마다 상단의 해당 밭 "LTE 통신 장비 연결 상태(라우터 상태)" 문구가 실시간으로 변하도록 설계했습니다.

## 4. 노지 센서 확장 권고안 (Zigbee vs Wi-Fi)
*   **결정 사항**: 향후 추가될 토양수분장력센서(4개)와 유량센서(2~3개)는 무조건 **지그비(Zigbee) 통신망**을 사용하기로 결론지었습니다.
*   **이유**:
    1.  **배터리**: 노지 한가운데 전기를 끌어올 수 없어 건전지로 작동해야 하는데, 와이파이는 전력 소모가 극심하여 불가능합니다.
    2.  **커버리지(Mesh)**: 귤나무 잎의 수분이 2.4GHz Wi-Fi 전파를 막아버립니다. 지그비는 센서들끼리 신호를 릴레이(토스)하는 Mesh 네트워크 특성이 있어 노지 구석구석 통신이 가능합니다.
*   **확장 방법**: 투야 지그비 게이트웨이(허브) 1대만 메인 라우터에 유선으로 물리면, OpenAPI 개발 스펙 자체는 Wi-Fi 기기와 100% 동일하므로 현재 `server.js`에 코드 몇 줄만 추가하여 즉시 센서 값을 대시보드에 뿌릴 수 있습니다.
